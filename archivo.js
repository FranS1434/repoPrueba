/*primero se debe relacionar el repositorio a la linea de
comandos de git ingresando a dicha carpeta. con git status
se corrobora que archivos esta siguiendo git.
con git add mas el nombre del archivo se relaciona el archivo
a seguir en git. a git add se le pueden pasar pasar de distintas
formas los parametros, una es con el nombre del arcivo, otra
es con ".mas la extension de todos los archivos a agregar
y por ultimo solo con . para todos los archivos con cualquier
extension. Tambien se pueden poner los nombres de todos los 
archivos uno al lado del otro. A ESTO SE LO CONOCE COMO 
AGREGAR A ESTANCIA DE STAGE

renombrar archivo = git mv nombreArchivo nuevoNombre
despues de esto se tiene que usar git add con los parametros
de los dos nombre de archivos, el que se va renmbrar mas el
nuevo nombre.
al igual que git rm evita tener que llamar a git  add para 
que se apliquen los cambios


elimianr archivo = git rm nombreArchivo. sirve para evitar
tener que llamar a git add para que se agreguen los cambios
que se realizaron en el archivo 

mover archivo = mv nombreArchivo desino

IGNORANDO ARCHIVOS

   



*/
console.log("hola mundo");

console.log("este este va ser otro cambio al archivo")
console.log("este este va ser otro cambio al archivo");
console.log("este este va ser otro cambio al archivo");